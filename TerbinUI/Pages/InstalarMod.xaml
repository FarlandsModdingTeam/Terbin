<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TerbinUI.Pages.InicioPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <LinearGradientBrush x:Key="HeroBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF101827" Offset="0"/>
            <GradientStop Color="#FF1F2937" Offset="1"/>
        </LinearGradientBrush>
    </Page.Resources>

    <Grid Background="{StaticResource HeroBackground}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border HorizontalAlignment="Center" VerticalAlignment="Center" Padding="32" CornerRadius="16"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="12" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Image Source="Assets/Wide310x150Logo.scale-200.png" Width="300" Stretch="Uniform"/>
                    <TextBlock Text="TerbinUI" Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Interfaz gr&#x00E1;fica ligera para Terbin" Opacity="0.8" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>