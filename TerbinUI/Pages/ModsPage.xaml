<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TerbinUI.Pages.ModsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <ScrollViewer>
        <Grid Padding="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Mods" Style="{StaticResource TitleTextBlockStyle}"/>

            <!-- Enlaces -->
            <StackPanel Grid.Row="1" Spacing="12">

                <!-- Zona de Web (Online) -->
                <TextBlock Text="Web (Online)" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <GridView x:Name="WebModsGrid" IsItemClickEnabled="True" ItemClick="OnModClick" SelectionMode="None">
                    <GridView.ItemsPanel>
                        
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                        
                    </GridView.ItemsPanel>
                    <GridView.ItemTemplate>
                        
                        <DataTemplate>
                            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="12" Margin="6" CornerRadius="8">
                                <StackPanel Spacing="4" Width="200">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextWrapping="WrapWholeWords"/>
                                    <TextBlock Text="{Binding Guid}" Opacity="0.6" FontSize="12"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                        
                    </GridView.ItemTemplate>
                </GridView>

                <!-- Zona de Local -->
                <TextBlock Text="Local" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,16,0,0"/>
                <GridView x:Name="LocalModsGrid" IsItemClickEnabled="True" ItemClick="OnModClick" SelectionMode="None">
                    <GridView.ItemsPanel>
                        
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                        
                    </GridView.ItemsPanel>
                    <GridView.ItemTemplate>
                        
                        <DataTemplate>
                            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="12" Margin="6" CornerRadius="8">
                                <StackPanel Spacing="4" Width="200">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextWrapping="WrapWholeWords"/>
                                    <TextBlock Text="{Binding Guid}" Opacity="0.6" FontSize="12"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                        
                    </GridView.ItemTemplate>
                </GridView>
            </StackPanel>
            
            <!-- Boton -->
            <Button Content="Agregar mod local"
                    Grid.Row="3"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Margin="0,16,0,0"
                    x:Name="BtnAgregarModLocal"/>
        </Grid>
    </ScrollViewer>
</Page>
